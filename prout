#include "fdf.h"

int		main(int ac, char **av)
{
	int		fd = open(av[1], O_RDONLY);
	int		g;
	char	*line;

	if (ac != 2)
		return (0);
	while ((g = get_next_line(fd, &line)) == 1)
	{
		printf("g = %d\n", g);
		printf("%s\n", line);
		free(line);
	}
	close(fd);

	return (0);
}
